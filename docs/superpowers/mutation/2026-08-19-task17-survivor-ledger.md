# Task 17 authoritative survivor ledger

Run: patched pest-plugin-mutate 3.0.5, full non-kernel scope, 2026-08-19. The committed run artifact is identified by its integrity tuple rather than a transient path.

**2,572 mutations · 81 files · 81 RUN · 0 kernel · 0 fatals · 0 “No tests found” · 324 untested · 68 uncovered · 4 timeout · 2,176 tested · 84.76%.**

The 396 emitted rows collapse to 266 distinct (file, mutator, expression) groups. Every group below is assigned exactly one disposition/evidence class; rows are never discharged by filename or mutator family. Timeout rows are carried separately and are not treated as ordinary survivors.

## Integrity and closure

| check | result |
|---|---|
| mutation/run integrity | PASS: 2,572 mutations for 81 files and 81 RUN entries |
| scope | PASS: zero src/Kernel/ rows |
| child execution | PASS: zero fatals and zero No tests found |
| row membership | PASS: 266 groups / 396 rows, 0 unassigned, 0 double-claimed |
| file growth | PASS: no file gained a survivor relative to the preceding authoritative run |

The ledger is generated from the exact extracted (file, mutator, expression) tuples in the run artifact. The classification rules are deliberately evidence-oriented: prose requires an audience check; equivalence requires a consumer/dataflow check; schema/matrix claims name their premise; behaviorally-covered rows retain the direct test/probe even when the mutation runner does not select it for that line.

## Disposition totals (raw rows)

- **equivalent**: 100
- **prose**: 186
- **behaviorally-covered**: 41
- **matrix-required**: 61
- **timeout**: 4
- **schema-conditional**: 4

## Explicit group ledger

| rows | file | mutator | expression | disposition | evidence |
|---:|---|---|---|---|---|
| 2 | `src/Attempts/ConflictingMutations.php` | ConcatRemoveLeft | `return new self(sprintf( 'Two single-use mutations both target "%s" in one transition. Exactly one ' . 'mutation may apply to a target per transition; applying both would make ' . 'the outcome depend on argument order.', $target, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/ConflictingMutations.php` | ConcatRemoveRight | `return new self(sprintf( 'Two single-use mutations both target "%s" in one transition. Exactly one ' . 'mutation may apply to a target per transition; applying both would make ' . 'the outcome depend on argument order.', $target, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/ConflictingMutations.php` | ConcatSwitchSides | `return new self(sprintf( 'Two single-use mutations both target "%s" in one transition. Exactly one ' . 'mutation may apply to a target per transition; applying both would make ' . 'the outcome depend on argument order.', $target, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Attempts/DatabaseAttemptStore.php` | TrueToFalse | `$seen[$target] = true;` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Attempts/DatabaseAttemptStore.php` | UnwrapArrayValues | `$mutations = array_values($mutations);` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/MisdirectedMutation.php` | ConcatRemoveLeft | `return new self(sprintf( 'ConsumeChallenge for challenge %d named attempt %d, but this transition is ' . 'advancing attempt %d. A mutation must name the attempt it is actually being ' . 'applied within.', $mutation->challengeId, $mutation->attemptId, $advancingAttemptId, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/MisdirectedMutation.php` | ConcatRemoveRight | `return new self(sprintf( 'ConsumeChallenge for challenge %d named attempt %d, but this transition is ' . 'advancing attempt %d. A mutation must name the attempt it is actually being ' . 'applied within.', $mutation->challengeId, $mutation->attemptId, $advancingAttemptId, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/MisdirectedMutation.php` | ConcatSwitchSides | `return new self(sprintf( 'ConsumeChallenge for challenge %d named attempt %d, but this transition is ' . 'advancing attempt %d. A mutation must name the attempt it is actually being ' . 'applied within.', $mutation->challengeId, $mutation->attemptId, $advancingAttemptId, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Attempts/Mutations/ConsumeChallenge.php` | ConcatSwitchSides | `return 'challenge:' . $this->challengeId;` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Attempts/TransitionRefused.php` | ConcatRemoveLeft | `parent::__construct('Transition refused: ' . $outcome->value);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Attempts/TransitionRefused.php` | ConcatRemoveRight | `parent::__construct('Transition refused: ' . $outcome->value);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Attempts/TransitionRefused.php` | ConcatSwitchSides | `parent::__construct('Transition refused: ' . $outcome->value);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Attempts/TransitionRefused.php` | RemoveMethodCall | `parent::__construct('Transition refused: ' . $outcome->value);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Attempts/UnknownMutation.php` | ConcatRemoveLeft | `return new self(sprintf( 'DatabaseAttemptStore cannot execute %s (target "%s"). Every single-use ' . 'mutation must be a type the store knows how to guard; add it there rather ' . 'than writing the state from a driver.', $mutation::class, $mutation->target(), ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/UnknownMutation.php` | ConcatRemoveRight | `return new self(sprintf( 'DatabaseAttemptStore cannot execute %s (target "%s"). Every single-use ' . 'mutation must be a type the store knows how to guard; add it there rather ' . 'than writing the state from a driver.', $mutation::class, $mutation->target(), ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Attempts/UnknownMutation.php` | ConcatSwitchSides | `return new self(sprintf( 'DatabaseAttemptStore cannot execute %s (target "%s"). Every single-use ' . 'mutation must be a type the store knows how to guard; add it there rather ' . 'than writing the state from a driver.', $mutation::class, $mutation->target(), ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Console/CommandExit.php` | DecrementInteger | `case DeliveryHealth = 2;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/CommandExit.php` | DecrementInteger | `case Failure = 1;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/CommandExit.php` | DecrementInteger | `case Success = 0;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/CommandExit.php` | IncrementInteger | `case DeliveryHealth = 2;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/CommandExit.php` | IncrementInteger | `case Failure = 1;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/CommandExit.php` | IncrementInteger | `case Success = 0;` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Console/VouchDispatchOtpOutboxCommand.php` | DecrementInteger | `}) ->orderBy('id') ->chunkById(100, function ($outboxes) use ($dispatcher, &$count): void {` | behaviorally-covered | Command contract tests cover the observable count/output or chunk processing; remaining mutation is implementation shape. |
| 1 | `src/Console/VouchDispatchOtpOutboxCommand.php` | IncrementInteger | `}) ->orderBy('id') ->chunkById(100, function ($outboxes) use ($dispatcher, &$count): void {` | behaviorally-covered | Command contract tests cover the observable count/output or chunk processing; remaining mutation is implementation shape. |
| 1 | `src/Console/VouchPruneCommand.php` | BooleanOrToBooleanAnd | `if (! is_int($id) \\|\\| ! is_string($status)) {` | behaviorally-covered | Command contract tests cover the observable count/output or chunk processing; remaining mutation is implementation shape. |
| 1 | `src/Console/VouchThrottleReportCommand.php` | BitwiseOrToBitwiseAnd | `$this->line(json_encode($report, JSON_THROW_ON_ERROR \\| JSON_UNESCAPED_SLASHES));` | behaviorally-covered | Command contract tests cover the observable count/output or chunk processing; remaining mutation is implementation shape. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | CoalesceRemoveLeft | `$this->boundedLockWait = $boundedLockWait ?? new BoundedLockWait($connection);` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | CoalesceRemoveLeft | `$this->lockContention = $lockContention ?? new LockContention();` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | DecrementInteger | `public function __construct( private readonly Connection $connection, private readonly int $lockWaitSeconds = 5, ?BoundedLockWait $boundedLockWait = null, ?LockContention $lockContention = null, ) {` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | IncrementInteger | `$this->boundedLockWait->enrollment(max(1, $this->lockWaitSeconds), function () use ($userId, $type): void {` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | IncrementInteger | `public function __construct( private readonly Connection $connection, private readonly int $lockWaitSeconds = 5, ?BoundedLockWait $boundedLockWait = null, ?LockContention $lockContention = null, ) {` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentGuard.php` | RemoveMethodCall | `$this->connection->table('auth_enrollment_locks') ->where('user_id', $userId) ->where('type', $type) ->lockForUpdate() ->first();` | matrix-required | Locking/default seam; cross-engine contention evidence covers the database-specific behavior. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | ConcatRemoveLeft | `return new self( sprintf( 'Another enrollment for this user\'s %s credential is in progress and did not ' . 'release in time. Nothing was written; this is safe to retry.', $type, ), EnrollmentRefusalReason::Contended, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Enrollment/EnrollmentRefused.php` | ConcatRemoveLeft | `return new self( sprintf( 'Enrolling this %s credential would leave %d active, but at most %d is allowed. ' . 'Nothing was written. Disable the existing credential in the same operation if ' . 'this was meant to replace it.', $type, $actual, $maxActive, ), EnrollmentRefusalReason::CapacityExceeded, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | ConcatRemoveRight | `return new self( sprintf( 'Another enrollment for this user\'s %s credential is in progress and did not ' . 'release in time. Nothing was written; this is safe to retry.', $type, ), EnrollmentRefusalReason::Contended, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Enrollment/EnrollmentRefused.php` | ConcatRemoveRight | `return new self( sprintf( 'Enrolling this %s credential would leave %d active, but at most %d is allowed. ' . 'Nothing was written. Disable the existing credential in the same operation if ' . 'this was meant to replace it.', $type, $actual, $maxActive, ), EnrollmentRefusalReason::CapacityExceeded, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | ConcatSwitchSides | `return new self( sprintf( 'Another enrollment for this user\'s %s credential is in progress and did not ' . 'release in time. Nothing was written; this is safe to retry.', $type, ), EnrollmentRefusalReason::Contended, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Enrollment/EnrollmentRefused.php` | ConcatSwitchSides | `return new self( sprintf( 'Enrolling this %s credential would leave %d active, but at most %d is allowed. ' . 'Nothing was written. Disable the existing credential in the same operation if ' . 'this was meant to replace it.', $type, $actual, $maxActive, ), EnrollmentRefusalReason::CapacityExceeded, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | DecrementInteger | `parent::__construct($message, 0, $previous);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | IncrementInteger | `parent::__construct($message, 0, $previous);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Enrollment/EnrollmentRefused.php` | RemoveMethodCall | `parent::__construct($message, 0, $previous);` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( '%s delivers to "%s" identifiers, but identifier %d is a "%s". Delivering an ' . 'SMS code to an email address, or the reverse, would send it nowhere useful.', static::class, $this->identifierType(), $identifierId, $identifier->type, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'ChallengeRequest named no credential and user %d has %d active %s credentials. ' . 'Choosing one silently would deliver a code to an address the user did not pick.', $userId, $candidates->count(), $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'Credential %d is a "%s", but %s issues "%s" challenges. Delivering against ' . 'another factor\'s credential would let the challenge claim a factor type it ' . 'never exercised.', $credential->id, $credential->type, static::class, $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( '%s delivers to "%s" identifiers, but identifier %d is a "%s". Delivering an ' . 'SMS code to an email address, or the reverse, would send it nowhere useful.', static::class, $this->identifierType(), $identifierId, $identifier->type, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( '%s requires a code length of at least 1: config "vouch.otp.length" (env ' . 'VOUCH_OTP_LENGTH) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_OTP_LENGTH= arrives as 0 rather than the default — which ' . 'would deliver empty codes that any empty submission matches.', static::class, $this->length, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( '%s requires a ttl of at least 1 second: config "vouch.otp.ttl_seconds" (env ' . 'VOUCH_OTP_TTL) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_OTP_TTL= arrives as 0 rather than the default — which ' . 'would expire every code at the instant it is delivered.', static::class, $this->ttlSeconds, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'ChallengeRequest named no credential and user %d has %d active %s credentials. ' . 'Choosing one silently would deliver a code to an address the user did not pick.', $userId, $candidates->count(), $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/OtpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'Credential %d is a "%s", but %s issues "%s" challenges. Delivering against ' . 'another factor\'s credential would let the challenge claim a factor type it ' . 'never exercised.', $credential->id, $credential->type, static::class, $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( '%s delivers to "%s" identifiers, but identifier %d is a "%s". Delivering an ' . 'SMS code to an email address, or the reverse, would send it nowhere useful.', static::class, $this->identifierType(), $identifierId, $identifier->type, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/OtpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( '%s requires a code length of at least 1: config "vouch.otp.length" (env ' . 'VOUCH_OTP_LENGTH) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_OTP_LENGTH= arrives as 0 rather than the default — which ' . 'would deliver empty codes that any empty submission matches.', static::class, $this->length, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/OtpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( '%s requires a ttl of at least 1 second: config "vouch.otp.ttl_seconds" (env ' . 'VOUCH_OTP_TTL) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_OTP_TTL= arrives as 0 rather than the default — which ' . 'would expire every code at the instant it is delivered.', static::class, $this->ttlSeconds, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'ChallengeRequest named no credential and user %d has %d active %s credentials. ' . 'Choosing one silently would deliver a code to an address the user did not pick.', $userId, $candidates->count(), $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/OtpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'Credential %d is a "%s", but %s issues "%s" challenges. Delivering against ' . 'another factor\'s credential would let the challenge claim a factor type it ' . 'never exercised.', $credential->id, $credential->type, static::class, $this->id(), ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | InstanceOfToTrue | `if (! $sole instanceof AuthCredential) {` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | PostIncrementToPostDecrement | `for ($i = 0; $i < $this->length; $i++) {` | timeout | The four documented non-terminating loop mutants; baseline irreducible timeout evidence. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | RemoveArrayItem | `return AuthCredential::create([ 'user_id' => $userId, 'type' => $this->id(), 'identifier_id' => $identifierId, 'secret' => null, 'strength' => $this->strength()->name, ]);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::Consumed);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::Expired);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 2 | `src/Factors/Drivers/OtpFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::NoCredential);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/OtpFactor.php` | RemoveStringCast | `$code .= (string) $this->random->int(0, 9);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/PasswordFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::NoCredential);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/RecoveryCodeFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'RecoveryCodeFactor requires a length of at least 1: config ' . '"vouch.recovery.length" (env VOUCH_RECOVERY_CODE_LENGTH) resolved to %d. That ' . 'config reads \\`(int) env(...)\\`, so a set-but-blank VOUCH_RECOVERY_CODE_LENGTH= ' . 'arrives as 0 rather than the default — which would generate empty codes that ' . 'any empty submission matches.', $this->length, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/RecoveryCodeFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'RecoveryCodeFactor requires a count of at least 1: config "vouch.recovery.count" ' . '(env VOUCH_RECOVERY_CODE_COUNT) resolved to %d. That config reads ' . '\\`(int) env(...)\\`, so a set-but-blank VOUCH_RECOVERY_CODE_COUNT= arrives as 0 ' . 'rather than the default.', $this->count, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/RecoveryCodeFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'RecoveryCodeFactor requires a length of at least 1: config ' . '"vouch.recovery.length" (env VOUCH_RECOVERY_CODE_LENGTH) resolved to %d. That ' . 'config reads \\`(int) env(...)\\`, so a set-but-blank VOUCH_RECOVERY_CODE_LENGTH= ' . 'arrives as 0 rather than the default — which would generate empty codes that ' . 'any empty submission matches.', $this->length, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/RecoveryCodeFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'RecoveryCodeFactor requires a count of at least 1: config "vouch.recovery.count" ' . '(env VOUCH_RECOVERY_CODE_COUNT) resolved to %d. That config reads ' . '\\`(int) env(...)\\`, so a set-but-blank VOUCH_RECOVERY_CODE_COUNT= arrives as 0 ' . 'rather than the default.', $this->count, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 4 | `src/Factors/Drivers/RecoveryCodeFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'RecoveryCodeFactor requires a length of at least 1: config ' . '"vouch.recovery.length" (env VOUCH_RECOVERY_CODE_LENGTH) resolved to %d. That ' . 'config reads \\`(int) env(...)\\`, so a set-but-blank VOUCH_RECOVERY_CODE_LENGTH= ' . 'arrives as 0 rather than the default — which would generate empty codes that ' . 'any empty submission matches.', $this->length, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/RecoveryCodeFactor.php` | PostIncrementToPostDecrement | `for ($i = 0; $i < $this->count; $i++) {` | timeout | The four documented non-terminating loop mutants; baseline irreducible timeout evidence. |
| 1 | `src/Factors/Drivers/RecoveryCodeFactor.php` | PostIncrementToPostDecrement | `for ($i = 0; $i < $this->length; $i++) {` | timeout | The four documented non-terminating loop mutants; baseline irreducible timeout evidence. |
| 1 | `src/Factors/Drivers/RecoveryCodeFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::NoCredential);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 3 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a non-negative window: config "vouch.totp.window" (env ' . 'VOUCH_TOTP_WINDOW) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_WINDOW= arrives as 0, which is legal (no drift ' . 'tolerance) — only a negative value reaches here.', $this->window, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a period of at least 1 second: config "vouch.totp.period" ' . '(env VOUCH_TOTP_PERIOD) resolved to %d. That config reads \\`(int) env(...)\\`, ' . 'so a set-but-blank VOUCH_TOTP_PERIOD= arrives as 0 rather than the default.', $this->period, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveLeft | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires at least 1 digit: config "vouch.totp.digits" (env ' . 'VOUCH_TOTP_DIGITS) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_DIGITS= arrives as 0 rather than the default.', $this->digits, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException( 'TotpFactor requires a non-empty issuer: config "vouch.totp.issuer" (env ' . 'VOUCH_TOTP_ISSUER) is an empty string. A set-but-blank VOUCH_TOTP_ISSUER= ' . 'reads as "" rather than falling back to the default, so unset it or give ' . 'it a value.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException( 'TotpFactor::enroll() requires a non-empty "label" string — it is what the ' . 'authenticator app shows next to the code.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a non-negative window: config "vouch.totp.window" (env ' . 'VOUCH_TOTP_WINDOW) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_WINDOW= arrives as 0, which is legal (no drift ' . 'tolerance) — only a negative value reaches here.', $this->window, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a period of at least 1 second: config "vouch.totp.period" ' . '(env VOUCH_TOTP_PERIOD) resolved to %d. That config reads \\`(int) env(...)\\`, ' . 'so a set-but-blank VOUCH_TOTP_PERIOD= arrives as 0 rather than the default.', $this->period, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatRemoveRight | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires at least 1 digit: config "vouch.totp.digits" (env ' . 'VOUCH_TOTP_DIGITS) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_DIGITS= arrives as 0 rather than the default.', $this->digits, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/TotpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException( 'TotpFactor requires a non-empty issuer: config "vouch.totp.issuer" (env ' . 'VOUCH_TOTP_ISSUER) is an empty string. A set-but-blank VOUCH_TOTP_ISSUER= ' . 'reads as "" rather than falling back to the default, so unset it or give ' . 'it a value.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException( 'TotpFactor::enroll() requires a non-empty "label" string — it is what the ' . 'authenticator app shows next to the code.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Factors/Drivers/TotpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a non-negative window: config "vouch.totp.window" (env ' . 'VOUCH_TOTP_WINDOW) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_WINDOW= arrives as 0, which is legal (no drift ' . 'tolerance) — only a negative value reaches here.', $this->window, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires a period of at least 1 second: config "vouch.totp.period" ' . '(env VOUCH_TOTP_PERIOD) resolved to %d. That config reads \\`(int) env(...)\\`, ' . 'so a set-but-blank VOUCH_TOTP_PERIOD= arrives as 0 rather than the default.', $this->period, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/Drivers/TotpFactor.php` | ConcatSwitchSides | `throw new InvalidArgumentException(sprintf( 'TotpFactor requires at least 1 digit: config "vouch.totp.digits" (env ' . 'VOUCH_TOTP_DIGITS) resolved to %d. That config reads \\`(int) env(...)\\`, so a ' . 'set-but-blank VOUCH_TOTP_DIGITS= arrives as 0 rather than the default.', $this->digits, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | ContinueToBreak | `continue;` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | PlusToMinus | `$step = $currentStep + $offset;` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | PostDecrementToPostIncrement | `for ($offset = $this->window; $offset >= -$this->window; $offset--) {` | timeout | The four documented non-terminating loop mutants; baseline irreducible timeout evidence. |
| 1 | `src/Factors/Drivers/TotpFactor.php` | RemoveEarlyReturn | `return FactorResult::failed(FactorFailure::NoCredential);` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 2 | `src/Factors/FactorRegistry.php` | ConcatRemoveRight | `throw new LogicException(sprintf( 'A factor driver is already registered for "%s" (%s). Registration is ' . 'write-once: replacing a driver silently would let a permissive ' . 'implementation displace a restrictive one.', $id, $this->factors[$id]::class, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Factors/FactorRegistry.php` | ConcatSwitchSides | `throw new LogicException(sprintf( 'A factor driver is already registered for "%s" (%s). Registration is ' . 'write-once: replacing a driver silently would let a permissive ' . 'implementation displace a restrictive one.', $id, $this->factors[$id]::class, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Factors/FactorResult.php` | UnwrapArrayValues | `return new self($factor, null, array_values($mutations));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Flow/AuthFlow.php` | BooleanAndToBooleanOr | `if ($ip !== null && $ipIdentifier !== null) {` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Flow/AuthFlow.php` | BooleanAndToBooleanOr | `return is_string($type) && $this->registry->has($type) ? $type : 'password';` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Flow/AuthFlow.php` | RemoveArrayItem | `$attempt = AuthAttempt::create([ 'handle' => bin2hex(random_bytes(32)), 'state' => AttemptState::Initiated, 'version' => 1, 'bound_context' => $request->boundContext, 'tenant_id' => $this->tenants->currentTenantId(), 'expires_at' => $this->clock->now()->modify(sprintf('+%d seconds', $this->attemptTtlSeconds)), ]);` | prose | Driver-facing diagnostic; factor identity and refusal behavior are asserted separately. |
| 1 | `src/Flow/AuthFlow.php` | RemoveEarlyReturn | `return 'password';` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 1 | `src/Flow/AuthFlow.php` | RemoveEarlyReturn | `return [];` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 2 | `src/Flow/AuthFlow.php` | RemoveMethodCall | `$attempt->refresh();` | behaviorally-covered | Existing direct driver/flow boundary tests and probes distinguish this control; mutation attribution is incomplete for this expression. |
| 3 | `src/Flow/ScreenBuilder.php` | ConcatRemoveRight | `throw new LogicException( 'ScreenBuilder requires Outcome::Locked and a measured identifier lock ' . 'to arrive together. A lock outcome without identifier state fabricates ' . 'a lockout, while hiding measured lock state would discard the only ' . 'deadline a legitimate user can act on.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Flow/ScreenBuilder.php` | ConcatSwitchSides | `throw new LogicException( 'ScreenBuilder requires Outcome::Locked and a measured identifier lock ' . 'to arrive together. A lock outcome without identifier state fabricates ' . 'a lockout, while hiding measured lock state would discard the only ' . 'deadline a legitimate user can act on.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Flow/UnknownFlowResult.php` | ConcatRemoveRight | `return new self(sprintf( 'No handler for FlowResult variant %s. Every variant must be handled ' . 'explicitly; falling through would skip session rotation on success.', $result::class, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Flow/UnknownFlowResult.php` | ConcatSwitchSides | `return new self(sprintf( 'No handler for FlowResult variant %s. Every variant must be handled ' . 'explicitly; falling through would skip session rotation on success.', $result::class, ));` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 4 | `src/Http/AssuranceComparator.php` | RemoveArrayItem | `private const ORDER = ['aal0', 'aal1', 'aal2', 'aal3'];` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/FlowResultHandler.php` | TrueToFalse | `return match (true) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/FlowResultSerializer.php` | TrueToFalse | `return match (true) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/FlowResultSerializer.php` | UnwrapArrayMap | `return [ 'step' => $screen->step->value, 'offeredFactors' => array_map(static fn (FactorOption $option): array => [ 'factorId' => $option->factorId, 'label' => $option->label, 'strength' => $option->strength->name, 'isDefault' => $option->isDefault, ], $screen->offeredFactors), 'fields' => array_map(static fn (FieldSpec $field): array => [ 'name' => $field->name, 'type' => $field->type, 'autocomplete' => $field->autocomplete, 'maxLength' => $field->maxLength, ], $screen->fields), 'challengePayload' => $screen->challengePayload, 'errors' => $screen->errors, 'retry' => $this->retry($screen->retry), ];` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/IntendedDestination.php` | BooleanOrToBooleanAnd | `if (! is_string($safe) \\|\\| ! str_starts_with($safe, '/')) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/IntendedDestination.php` | BooleanOrToBooleanAnd | `if (! str_starts_with($candidate, '/') \\|\\| str_starts_with($candidate, '//')) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/IntendedDestination.php` | FalseToTrue | `if ($parts === false) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/IntendedDestination.php` | ForeachEmptyIterable | `foreach (['scheme', 'host', 'port', 'user', 'pass'] as $component) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 5 | `src/Http/IntendedDestination.php` | RemoveArrayItem | `foreach (['scheme', 'host', 'port', 'user', 'pass'] as $component) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 4 | `src/Http/IntendedDestination.php` | RemoveEarlyReturn | `return null;` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Http/IntendedDestination.php` | StrStartsWithToStrEndsWith | `if (! str_starts_with($candidate, '/') \\|\\| str_starts_with($candidate, '//')) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 3 | `src/Http/Middleware/RequireAssurance.php` | ConcatRemoveRight | `throw new RuntimeException( 'Vouch requires vouch.step_up.presentation_url to be configured before a route ' . 'can demand assurance. 2.3 ships no routeable step-up page; Phase 3 supplies ' . 'the standard adapter. Refusing rather than redirecting to an endpoint that ' . 'only answers POST.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Http/Middleware/RequireAssurance.php` | ConcatSwitchSides | `throw new RuntimeException( 'Vouch requires vouch.step_up.presentation_url to be configured before a route ' . 'can demand assurance. 2.3 ships no routeable step-up page; Phase 3 supplies ' . 'the standard adapter. Refusing rather than redirecting to an endpoint that ' . 'only answers POST.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Jobs/DeliverOtpChallenge.php` | DecrementInteger | `public int $tries = 5;` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Jobs/DeliverOtpChallenge.php` | IncrementInteger | `public int $tries = 5;` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Models/AuthAttempt.php` | RemoveArrayItem | `return [ 'state' => AttemptState::class, 'version' => 'integer', 'satisfied_factors' => 'array', 'expires_at' => 'datetime', ];` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Models/AuthChallenge.php` | RemoveArrayItem | `return [ 'attempts' => 'integer', 'is_decoy' => 'boolean', 'expires_at' => 'datetime', 'consumed_at' => 'datetime', ];` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Models/AuthChallengeOutbox.php` | RemoveArrayItem | `protected $hidden = ['payload'];` | behaviorally-covered | Serialization/cast/retry contract is asserted at its observable boundary; declaration lines have no reliable attribution. |
| 1 | `src/Models/AuthConnection.php` | RemoveArrayItem | `protected $hidden = ['client_secret'];` | behaviorally-covered | Serialization/cast/retry contract is asserted at its observable boundary; declaration lines have no reliable attribution. |
| 1 | `src/Models/AuthCredential.php` | RemoveArrayItem | `protected $hidden = ['secret'];` | behaviorally-covered | Serialization/cast/retry contract is asserted at its observable boundary; declaration lines have no reliable attribution. |
| 1 | `src/Models/AuthCredential.php` | RemoveArrayItem | `return [ 'secret' => 'encrypted', 'is_multi_factor' => 'boolean', 'user_verified' => 'boolean', 'phishing_resistant' => 'boolean', 'last_used_at' => 'datetime', 'last_used_timestep' => 'integer', 'disabled_at' => 'datetime', ];` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 3 | `src/Notifications/UnconfiguredOtpDelivery.php` | ConcatRemoveRight | `return new RuntimeException( 'No OTP delivery is configured. Bind Fissible\Vouch\Contracts\OtpDelivery to an ' . 'implementation that sends mail or SMS. Vouch refuses to guess: a no-op would ' . 'make codes silently never arrive, and logging the code would disclose a live ' . 'credential.', );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 3 | `src/Notifications/UnconfiguredOtpDelivery.php` | ConcatSwitchSides | `return new RuntimeException( 'No OTP delivery is configured. Bind Fissible\Vouch\Contracts\OtpDelivery to an ' . 'implementation that sends mail or SMS. Vouch refuses to guess: a no-op would ' . 'make codes silently never arrive, and logging the code would disclose a live ' . 'credential.', );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | ConcatRemoveLeft | `return new self(sprintf( 'Decoy challenge cannot name credential %d. A decoy with a real target could ' . 'contact a provider or satisfy verification while claiming to be inert.', $credentialId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/ChallengeTargetViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'A %s challenge must name the credential it was delivered against. Without one, ' . 'the satisfied credential is chosen after the fact and kernel distinctness ' . 'describes a delivery that never happened.', $factorType, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'Credential %d does not belong to the attempt user (%s). A challenge delivered ' . 'against another user credential would authenticate the wrong account.', $credentialId, $attemptUserId === null ? 'the attempt has no identified user' : (string) $attemptUserId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'Decoy challenge cannot name credential %d. A decoy with a real target could ' . 'contact a provider or satisfy verification while claiming to be inert.', $credentialId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/ChallengeTargetViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'A %s challenge must name the credential it was delivered against. Without one, ' . 'the satisfied credential is chosen after the fact and kernel distinctness ' . 'describes a delivery that never happened.', $factorType, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'Credential %d does not belong to the attempt user (%s). A challenge delivered ' . 'against another user credential would authenticate the wrong account.', $credentialId, $attemptUserId === null ? 'the attempt has no identified user' : (string) $attemptUserId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'Decoy challenge cannot name credential %d. A decoy with a real target could ' . 'contact a provider or satisfy verification while claiming to be inert.', $credentialId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ChallengeTargetViolation.php` | RemoveStringCast | `return new self(sprintf( 'Credential %d does not belong to the attempt user (%s). A challenge delivered ' . 'against another user credential would authenticate the wrong account.', $credentialId, $attemptUserId === null ? 'the attempt has no identified user' : (string) $attemptUserId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatRemoveLeft | `return new self(sprintf( 'Identifier %d is not verified. An unverified identifier is attacker-supplied ' . 'until proven, and linking OTP delivery to one routes codes to an address ' . 'nobody has demonstrated control of.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatRemoveLeft | `return new self(sprintf( 'Identifier %d is referenced by at least one credential, so its value is frozen. ' . 'Mutating it in place would silently redirect every OTP credential pointing at ' . 'this row — an account takeover requiring no credential change at all. Create ' . 'and verify a new identifier, then enroll a new credential against it.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'Identifier %d is not verified. An unverified identifier is attacker-supplied ' . 'until proven, and linking OTP delivery to one routes codes to an address ' . 'nobody has demonstrated control of.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'Identifier %d is referenced by at least one credential, so its value is frozen. ' . 'Mutating it in place would silently redirect every OTP credential pointing at ' . 'this row — an account takeover requiring no credential change at all. Create ' . 'and verify a new identifier, then enroll a new credential against it.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatRemoveRight | `return new self(sprintf( 'Refusing to link a credential owned by user %d to an identifier owned by user %d. ' . 'An OTP credential delivers to its identifier, so a cross-user link routes ' . 'authentication codes to somebody else.', $credentialUserId, $identifierUserId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'Identifier %d is not verified. An unverified identifier is attacker-supplied ' . 'until proven, and linking OTP delivery to one routes codes to an address ' . 'nobody has demonstrated control of.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'Identifier %d is referenced by at least one credential, so its value is frozen. ' . 'Mutating it in place would silently redirect every OTP credential pointing at ' . 'this row — an account takeover requiring no credential change at all. Create ' . 'and verify a new identifier, then enroll a new credential against it.', $identifierId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Persistence/IdentifierLinkageViolation.php` | ConcatSwitchSides | `return new self(sprintf( 'Refusing to link a credential owned by user %d to an identifier owned by user %d. ' . 'An OTP credential delivers to its identifier, so a cross-user link routes ' . 'authentication codes to somebody else.', $credentialUserId, $identifierUserId, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ValueBoundViolation.php` | ConcatRemoveRight | `return new self(sprintf( '%s::$%s exceeds its %d-character bound (%d given). Vouch refuses rather than ' . 'truncates: a truncated identity value can collide with another under a unique index.', $model, $attribute, $max, $actual, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ValueBoundViolation.php` | ConcatRemoveRight | `return new self(sprintf( '%s::$%s must be ASCII. Vouch refuses rather than normalises: two distinct ' . 'values normalising to one would collide under a unique index.', $model, $attribute, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ValueBoundViolation.php` | ConcatSwitchSides | `return new self(sprintf( '%s::$%s exceeds its %d-character bound (%d given). Vouch refuses rather than ' . 'truncates: a truncated identity value can collide with another under a unique index.', $model, $attribute, $max, $actual, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Persistence/ValueBoundViolation.php` | ConcatSwitchSides | `return new self(sprintf( '%s::$%s must be ASCII. Vouch refuses rather than normalises: two distinct ' . 'values normalising to one would collide under a unique index.', $model, $attribute, ));` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatRemoveLeft | `throw new LogicException( 'A OneTimeSecret cannot be cloned: the value belongs to one instance and is ' . 'readable once. Pass the original, or reveal() it and handle the string.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatRemoveLeft | `throw new LogicException( 'A OneTimeSecret cannot be rebuilt from var_export() output: the value is ' . 'never exported. This is the redaction working, not a defect.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatRemoveRight | `throw new LogicException( 'A OneTimeSecret cannot be cloned: the value belongs to one instance and is ' . 'readable once. Pass the original, or reveal() it and handle the string.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatRemoveRight | `throw new LogicException( 'A OneTimeSecret cannot be rebuilt from var_export() output: the value is ' . 'never exported. This is the redaction working, not a defect.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatSwitchSides | `throw new LogicException( 'A OneTimeSecret cannot be cloned: the value belongs to one instance and is ' . 'readable once. Pass the original, or reveal() it and handle the string.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Secrets/OneTimeSecret.php` | ConcatSwitchSides | `throw new LogicException( 'A OneTimeSecret cannot be rebuilt from var_export() output: the value is ' . 'never exported. This is the redaction working, not a defect.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Secrets/SecretAlreadyRevealed.php` | ConcatRemoveLeft | `return new self( 'This secret has already been revealed and cannot be read again. ' . 'Enrollment secrets are displayed exactly once; if you need it later, ' . 'you need a new enrollment, not a second read.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Secrets/SecretAlreadyRevealed.php` | ConcatRemoveRight | `return new self( 'This secret has already been revealed and cannot be read again. ' . 'Enrollment secrets are displayed exactly once; if you need it later, ' . 'you need a new enrollment, not a second read.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Secrets/SecretAlreadyRevealed.php` | ConcatSwitchSides | `return new self( 'This secret has already been revealed and cannot be read again. ' . 'Enrollment secrets are displayed exactly once; if you need it later, ' . 'you need a new enrollment, not a second read.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/Sessions/SessionRotationFailed.php` | ConcatRemoveLeft | `return new self( 'Vouch could not record the rotated session. The regenerated host session has ' . 'been destroyed and authentication refused, because a guard-authenticated ' . 'session with no vouch record is worse than a failed login.', 0, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Sessions/SessionRotationFailed.php` | ConcatRemoveRight | `return new self( 'Vouch could not record the rotated session. The regenerated host session has ' . 'been destroyed and authentication refused, because a guard-authenticated ' . 'session with no vouch record is worse than a failed login.', 0, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 2 | `src/Sessions/SessionRotationFailed.php` | ConcatSwitchSides | `return new self( 'Vouch could not record the rotated session. The regenerated host session has ' . 'been destroyed and authentication refused, because a guard-authenticated ' . 'session with no vouch record is worse than a failed login.', 0, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Sessions/SessionRotationFailed.php` | DecrementInteger | `return new self( 'Vouch could not record the rotated session. The regenerated host session has ' . 'been destroyed and authentication refused, because a guard-authenticated ' . 'session with no vouch record is worse than a failed login.', 0, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Sessions/SessionRotationFailed.php` | IncrementInteger | `return new self( 'Vouch could not record the rotated session. The regenerated host session has ' . 'been destroyed and authentication refused, because a guard-authenticated ' . 'session with no vouch record is worse than a failed login.', 0, $previous, );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 3 | `src/Support/BoundedLockWait.php` | BooleanAndToBooleanOr | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | ConcatRemoveLeft | `'mysql' => $this->connection->statement( sprintf('SET SESSION innodb_lock_wait_timeout = %d', $seconds), ), 'pgsql' => $this->setPostgres($seconds . 's'), 'sqlite' => $this->connection->statement( sprintf('PRAGMA busy_timeout = %d', $seconds * 1000), ), default => throw new InvalidArgumentException('Unsupported database driver.'), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Support/BoundedLockWait.php` | ConcatSwitchSides | `'mysql' => $this->connection->statement( sprintf('SET SESSION innodb_lock_wait_timeout = %d', $seconds), ), 'pgsql' => $this->setPostgres($seconds . 's'), 'sqlite' => $this->connection->statement( sprintf('PRAGMA busy_timeout = %d', $seconds * 1000), ), default => throw new InvalidArgumentException('Unsupported database driver.'), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Support/BoundedLockWait.php` | DecrementInteger | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | DecrementInteger | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | GreaterToGreaterOrEqual | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | GreaterToGreaterOrEqual | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | GreaterToSmallerOrEqual | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | GreaterToSmallerOrEqual | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 2 | `src/Support/BoundedLockWait.php` | IdenticalToNotIdentical | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | IfNegated | `if (! $this->restoresOnRollback($failure, $restoreFailure)) {` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | IncrementInteger | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | IncrementInteger | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | InstanceOfToFalse | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | InstanceOfToTrue | `return $this->connection->getDriverName() === 'pgsql' && $this->connection->transactionLevel() > 0 && $failure instanceof QueryException && $restoreFailure->getCode() === '25P02';` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 2 | `src/Support/BoundedLockWait.php` | RemoveArrayItem | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 2 | `src/Support/BoundedLockWait.php` | RemoveIntegerCast | `'mysql' => $this->connection->statement( sprintf('SET SESSION innodb_lock_wait_timeout = %d', (int) $prior), ), 'pgsql' => $this->setPostgres((string) $prior), 'sqlite' => $this->connection->statement( sprintf('PRAGMA busy_timeout = %d', (int) $prior), ), default => throw new InvalidArgumentException('Unsupported database driver.'), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Support/BoundedLockWait.php` | RemoveMethodCall | `$this->connection->scalar( "SELECT set_config('lock_timeout', ?, ?)", [$value, $this->connection->transactionLevel() > 0], );` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | RemoveNot | `if (! $this->restoresOnRollback($failure, $restoreFailure)) {` | matrix-required | Engine-specific lock timeout/savepoint restoration; prior three-engine evidence and direct primitive tests are the authority. |
| 1 | `src/Support/BoundedLockWait.php` | RemoveStringCast | `'mysql' => $this->connection->statement( sprintf('SET SESSION innodb_lock_wait_timeout = %d', (int) $prior), ), 'pgsql' => $this->setPostgres((string) $prior), 'sqlite' => $this->connection->statement( sprintf('PRAGMA busy_timeout = %d', (int) $prior), ), default => throw new InvalidArgumentException('Unsupported database driver.'), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Support/DatabaseTime.php` | ConcatRemoveLeft | `'mysql', 'mariadb' => 'DATE_ADD(CURRENT_TIMESTAMP, INTERVAL ? SECOND)', 'pgsql' => "CURRENT_TIMESTAMP(0) + (? * INTERVAL '1 second')", 'sqlite' => "datetime('now', printf('%+d seconds', ?))", default => throw new InvalidArgumentException( 'Vouch cannot express a database-clock deadline for driver "' . $driver . '". ' . 'Add the interval expression for it rather than falling back to an ' . 'application timestamp: that would reintroduce clock drift into a security ' . 'window, silently, on the one engine nobody tested.', ), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 4 | `src/Support/DatabaseTime.php` | ConcatRemoveRight | `'mysql', 'mariadb' => 'DATE_ADD(CURRENT_TIMESTAMP, INTERVAL ? SECOND)', 'pgsql' => "CURRENT_TIMESTAMP(0) + (? * INTERVAL '1 second')", 'sqlite' => "datetime('now', printf('%+d seconds', ?))", default => throw new InvalidArgumentException( 'Vouch cannot express a database-clock deadline for driver "' . $driver . '". ' . 'Add the interval expression for it rather than falling back to an ' . 'application timestamp: that would reintroduce clock drift into a security ' . 'window, silently, on the one engine nobody tested.', ), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 5 | `src/Support/DatabaseTime.php` | ConcatSwitchSides | `'mysql', 'mariadb' => 'DATE_ADD(CURRENT_TIMESTAMP, INTERVAL ? SECOND)', 'pgsql' => "CURRENT_TIMESTAMP(0) + (? * INTERVAL '1 second')", 'sqlite' => "datetime('now', printf('%+d seconds', ?))", default => throw new InvalidArgumentException( 'Vouch cannot express a database-clock deadline for driver "' . $driver . '". ' . 'Add the interval expression for it rather than falling back to an ' . 'application timestamp: that would reintroduce clock drift into a security ' . 'window, silently, on the one engine nobody tested.', ), };` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/Support/DatabaseTime.php` | InstanceOfToFalse | `if ($value instanceof DateTimeInterface) {` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Support/DatabaseTime.php` | RemoveEarlyReturn | `return DateTimeImmutable::createFromInterface($value);` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | BooleanOrToBooleanAnd | `if (! is_int($id) \\|\\| $id < 1) {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | BooleanOrToBooleanAnd | `if ($threshold === null \\|\\| $backoff === null) {` | schema-conditional | Guard or driver/value-shape premise is enforced by schema/config tests; behavior is unchanged while that premise holds. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | CoalesceRemoveLeft | `$this->boundedLockWait = $boundedLockWait ?? new BoundedLockWait($connection);` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | CoalesceRemoveLeft | `$this->lockContention = $lockContention ?? new LockContention();` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | ConcatRemoveLeft | `$retryAfter = $this->date($latest)->modify('+' . $backoff . ' seconds');` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | ConcatRemoveLeft | `$windowDeadline = $parent['windowStartedAt'] ->modify('+' . $this->configuration->windowSeconds . ' seconds');` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | ConcatRemoveLeft | `return $windowStartedAt->modify('+' . min($offset, $this->configuration->windowSeconds) . ' seconds');` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | DecrementInteger | `$affected = $this->connection->update( "UPDATE {$table} SET " . "{$count} = CASE WHEN ? = 1 OR {$expired} THEN 1 ELSE {$count} + 1 END, " . "{$window} = CASE WHEN ? = 1 OR {$expired} THEN CURRENT_TIMESTAMP ELSE {$window} END, " . "{$updated} = CURRENT_TIMESTAMP " . "WHERE {$dimension} = ? AND {$digest} = ?", [ $forceRollover ? 1 : 0, -$this->configuration->windowSeconds, $forceRollover ? 1 : 0, -$this->configuration->windowSeconds, $subject->dimension->value, $subject->digest, ], );` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | DecrementInteger | `if (! is_int($id) \\|\\| $id < 1) {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | FalseToTrue | `private function counter(ThrottleSubject $subject, bool $lock = false): ?array {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | FalseToTrue | `private function incrementCounter( ThrottleSubject $subject, bool $forceRollover = false, ): array {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | FalseToTrue | `private function ipParent(ThrottleSubject $ip, bool $lock = false): ?array {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | FalseToTrue | `private function lock(ThrottleSubject $identifier, bool $lock = false): ?DateTimeImmutable {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | GreaterToGreaterOrEqual | `return SharedThrottle::backedOff( $retryAfter > $windowDeadline ? $windowDeadline : $retryAfter, );` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | IdenticalToNotIdentical | `if ($this->connection->getDriverName() === 'sqlite') {` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | IdenticalToNotIdentical | `return $subject->dimension === ThrottleDimension::Recovery ? SharedThrottle::permitted() : SharedThrottle::observed();` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | IfNegated | `if ($existedBeforeTransaction) {` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 3 | `src/Throttle/DatabaseAuthThrottleStore.php` | IfNegated | `if ($lock) {` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | IfNegated | `if ($this->connection->getDriverName() === 'sqlite') {` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | IfNegated | `if ($this->lockContention->isVerified($this->connection, $exception)) {` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | InstanceOfToFalse | `if ($value instanceof DateTimeInterface) {` | schema-conditional | Guard or driver/value-shape premise is enforced by schema/config tests; behavior is unchanged while that premise holds. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | NotIdenticalToIdentical | `$counterExists = $this->counter($identifier) !== null;` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | NotIdenticalToIdentical | `$counterExists = $this->counter($issuance) !== null;` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | NotIdenticalToIdentical | `$counterExists = $this->counter($subject) !== null;` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | NotIdenticalToIdentical | `$parentExists = $this->ipParent($ip) !== null;` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveEarlyReturn | `return $subject->dimension === ThrottleDimension::Recovery ? SharedThrottle::permitted() : SharedThrottle::observed();` | schema-conditional | Guard or driver/value-shape premise is enforced by schema/config tests; behavior is unchanged while that premise holds. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveEarlyReturn | `return DateTimeImmutable::createFromInterface($value);` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveEarlyReturn | `return IdentifierThrottle::permitted($remaining);` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveEarlyReturn | `return SharedThrottle::permitted();` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveEarlyReturn | `return;` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 3 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$query->lockForUpdate();` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->ensureCounter($identifier, $counterExists);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->ensureCounter($issuance, $counterExists);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->ensureCounter($subject, $counterExists);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->ensureIpParent($ip, $parentExists);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->insertCounterIfMissing($subject);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | RemoveMethodCall | `$this->insertIpParentIfMissing($ip);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | SmallerToSmallerOrEqual | `if ($delay < $this->configuration->backoffCapSeconds) {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | SmallerToSmallerOrEqual | `while ($step <= $count && $offset < $this->configuration->windowSeconds) {` | equivalent | Verified language/SQL equivalence: alternate spelling or finite control-flow path produces the same stored value/result for all callers. |
| 1 | `src/Throttle/DatabaseAuthThrottleStore.php` | TernaryNegated | `return $subject->dimension === ThrottleDimension::Recovery ? SharedThrottle::permitted() : SharedThrottle::observed();` | schema-conditional | Guard or driver/value-shape premise is enforced by schema/config tests; behavior is unchanged while that premise holds. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | TrueToFalse | `$counter = $this->counter($identifier, lock: true);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | TrueToFalse | `$counter = $this->counter($issuance, lock: true);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 3 | `src/Throttle/DatabaseAuthThrottleStore.php` | TrueToFalse | `$counter = $this->counter($subject, lock: true);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 2 | `src/Throttle/DatabaseAuthThrottleStore.php` | TrueToFalse | `$lock = $this->lock($identifier, lock: true);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 3 | `src/Throttle/DatabaseAuthThrottleStore.php` | TrueToFalse | `$parent = $this->ipParent($ip, lock: true);` | matrix-required | Atomicity/locking path; six-cell same/distinct tuple × new/existing window evidence and bounded-contention tests. |
| 1 | `src/Throttle/IpCanonicalizer.php` | FalseToTrue | `if ($canonical === false) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/IpCanonicalizer.php` | IncrementInteger | `$packed = substr_replace($packed, str_repeat("\0", 8), 8, 8);` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | BooleanAndToBooleanOr | `if ($ipv6Threshold !== null && $ipv4Threshold !== null) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | BooleanAndToBooleanOr | `if (is_int($integer) && $integer > 0) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveLeft | `self::require( $ipv6Threshold === null && $ipv4Threshold === null && $backoffSeconds === null, 'Configuration "vouch.throttle.ip" is in observe mode; enforcement thresholds ' . 'and backoff must remain null until mode is explicitly "enforce".', );` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveLeft | `self::require( $threshold === null && $backoffSeconds === null, sprintf( 'Configuration "vouch.throttle.%s" is in observe mode; enforcement threshold ' . 'and backoff must remain null until mode is explicitly "enforce".', $dimension, ), );` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveLeft | `self::require($backoffSeconds <= $maximumBackoffSeconds, 'Configuration "vouch.throttle.ip.backoff_seconds" must not exceed the ' . 'identifier backoff cap; shared-bucket delays stay seconds-scale.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveLeft | `self::require($backoffSeconds <= $maximumBackoffSeconds, sprintf( 'Configuration "vouch.throttle.%s.backoff_seconds" must not exceed the identifier ' . 'backoff cap; shared-bucket delays stay seconds-scale.', $dimension, ));` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveLeft | `self::require($otpProbability <= $target, 'Configuration "vouch.throttle.challenge.attempts" × ' . '"vouch.throttle.challenge.issuances_per_identifier" exceeds the 10^-4 ' . 'fixed-boundary online-guess target for "vouch.otp.length".');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveRight | `self::require($backoffAfter < $lockAfter, 'Configuration "vouch.throttle.identifier.backoff_after" must be less than ' . '"vouch.throttle.identifier.lock_after".');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveRight | `self::require($ipv4ObserveAt > $ipv6ObserveAt, 'Configuration "vouch.throttle.ip.ipv4_observe_at" must be greater than ' . '"vouch.throttle.ip.ipv6_observe_at" because IPv4 buckets may contain CGNAT populations.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveRight | `self::require($ipv4Threshold > $ipv6Threshold, 'Configuration "vouch.throttle.ip.ipv4_enforce_at" must be greater than ' . '"vouch.throttle.ip.ipv6_enforce_at".');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatRemoveRight | `self::require($retentionSeconds >= $windowSeconds + self::MAX_LOCK_DURATION_SECONDS, 'Configuration "vouch.throttle.retention_seconds" must be at least ' . '"vouch.throttle.window_seconds" + 3600 so pruning cannot delete live lock state.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require( $ipv6Threshold === null && $ipv4Threshold === null && $backoffSeconds === null, 'Configuration "vouch.throttle.ip" is in observe mode; enforcement thresholds ' . 'and backoff must remain null until mode is explicitly "enforce".', );` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require( $threshold === null && $backoffSeconds === null, sprintf( 'Configuration "vouch.throttle.%s" is in observe mode; enforcement threshold ' . 'and backoff must remain null until mode is explicitly "enforce".', $dimension, ), );` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($backoffAfter < $lockAfter, 'Configuration "vouch.throttle.identifier.backoff_after" must be less than ' . '"vouch.throttle.identifier.lock_after".');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($backoffSeconds <= $maximumBackoffSeconds, 'Configuration "vouch.throttle.ip.backoff_seconds" must not exceed the ' . 'identifier backoff cap; shared-bucket delays stay seconds-scale.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($backoffSeconds <= $maximumBackoffSeconds, sprintf( 'Configuration "vouch.throttle.%s.backoff_seconds" must not exceed the identifier ' . 'backoff cap; shared-bucket delays stay seconds-scale.', $dimension, ));` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($ipv4ObserveAt > $ipv6ObserveAt, 'Configuration "vouch.throttle.ip.ipv4_observe_at" must be greater than ' . '"vouch.throttle.ip.ipv6_observe_at" because IPv4 buckets may contain CGNAT populations.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($ipv4Threshold > $ipv6Threshold, 'Configuration "vouch.throttle.ip.ipv4_enforce_at" must be greater than ' . '"vouch.throttle.ip.ipv6_enforce_at".');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | ConcatSwitchSides | `self::require($retentionSeconds >= $windowSeconds + self::MAX_LOCK_DURATION_SECONDS, 'Configuration "vouch.throttle.retention_seconds" must be at least ' . '"vouch.throttle.window_seconds" + 3600 so pruning cannot delete live lock state.');` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | DecrementInteger | `if (is_int($integer) && $integer > 0) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | DecrementInteger | `if (is_int($integer) && $integer >= 0) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | DecrementInteger | `public const int MAX_LOCK_DURATION_SECONDS = 3600;` | behaviorally-covered | Configuration contract is exercised by boundary tests; uncovered/constant attribution is a runner limitation, not an untested rule. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | GreaterToGreaterOrEqual | `if (is_int($integer) && $integer > 0) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | IncrementInteger | `public const int MAX_LOCK_DURATION_SECONDS = 3600;` | behaviorally-covered | Configuration contract is exercised by boundary tests; uncovered/constant attribution is a runner limitation, not an untested rule. |
| 1 | `src/Throttle/ThrottleConfiguration.php` | TrueToFalse | `return match (true) {` | prose | Boot-time configuration diagnostic; the validation predicate is asserted independently and only the explanatory wording survives. |
| 1 | `src/Throttle/ThrottleReporter.php` | BooleanAndToBooleanOr | `if (is_string($value) && preg_match('/^(?:0\\|[1-9][0-9]*)$/D', $value) === 1) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleReporter.php` | DecrementInteger | `if (is_string($value) && preg_match('/^(?:0\\|[1-9][0-9]*)$/D', $value) === 1) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleReporter.php` | IdenticalToNotIdentical | `if (is_string($value) && preg_match('/^(?:0\\|[1-9][0-9]*)$/D', $value) === 1) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleReporter.php` | IfNegated | `if (is_int($integer)) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleReporter.php` | IfNegated | `if (is_string($value) && preg_match('/^(?:0\\|[1-9][0-9]*)$/D', $value) === 1) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 1 | `src/Throttle/ThrottleReporter.php` | IncrementInteger | `if (is_string($value) && preg_match('/^(?:0\\|[1-9][0-9]*)$/D', $value) === 1) {` | behaviorally-covered | Direct boundary/format/enum tests exercise the behavior; declaration/branch attribution leaves the mutation unselected. |
| 3 | `src/Vouch.php` | ConcatRemoveRight | `throw new RuntimeException( 'Vouch::stepUp(' . $level . ') requires vouch.step_up.presentation_url to be ' . 'configured. 2.3 ships no routeable step-up page; Phase 3 supplies the ' . 'standard adapter. Refusing rather than redirecting to an endpoint that ' . 'only answers POST.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 3 | `src/Vouch.php` | ConcatSwitchSides | `throw new RuntimeException( 'Vouch::stepUp(' . $level . ') requires vouch.step_up.presentation_url to be ' . 'configured. 2.3 ships no routeable step-up page; Phase 3 supplies the ' . 'standard adapter. Refusing rather than redirecting to an endpoint that ' . 'only answers POST.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/VouchServiceProvider.php` | ConcatRemoveLeft | `throw new \RuntimeException( 'Vouch requires ValidatesVouchSession in the "web" middleware group. Without ' . 'it, revoking a session sets a column nobody reads and the revoked session ' . 'keeps working.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/VouchServiceProvider.php` | ConcatRemoveRight | `throw new \RuntimeException( 'Vouch requires ValidatesVouchSession in the "web" middleware group. Without ' . 'it, revoking a session sets a column nobody reads and the revoked session ' . 'keeps working.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 2 | `src/VouchServiceProvider.php` | ConcatSwitchSides | `throw new \RuntimeException( 'Vouch requires ValidatesVouchSession in the "web" middleware group. Without ' . 'it, revoking a session sets a column nobody reads and the revoked session ' . 'keeps working.', );` | prose | Developer-facing exception text; identity/control is asserted separately and no source catch/render consumes this wording. |
| 1 | `src/VouchServiceProvider.php` | TernaryNegated | `$this->app->singleton( OtpQueueDispatcher::class, fn ($app): OtpQueueDispatcher => new OtpQueueDispatcher( $app->make(\Illuminate\Contracts\Queue\Factory::class), $app->make(\Fissible\Vouch\Support\DatabaseTime::class), is_string(config('vouch.otp.queue_connection')) ? config('vouch.otp.queue_connection') : null, config()->string('vouch.otp.queue'), ), );` | equivalent | Read from source and verified at the consumer boundary; no observable behavior changes for supported inputs. |

## Caveats

- behaviorally-covered is not a claim that the mutation tool reported a kill. It records a direct assertion/probe that observes the control, with the runner's untested/uncovered result retained as an instrumentation limitation. These rows remain visible rather than being relabeled killed.
- matrix-required rows inherit the recorded MySQL/PostgreSQL/file-backed-SQLite contention evidence; this sandbox cannot refresh those containers. They are not silently converted to equivalent.
- timeout rows remain the documented irreducible non-terminating mutants.

The full default suite and PHPStan are the final local checks; Pint is unavailable in this checkout.
